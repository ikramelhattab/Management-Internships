
{# intégrer code de l'entete dans le fichier _header.html.twig    #}

{% include "_header.html.twig" %}

{#   title de page  #}

{% block title %}{% endblock %}

{% block body %}
<br><br>
    <h1>Categoreis</h1>
<br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorei.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorei.name }}</td>
            </tr>
        </tbody>
    </table>
{# retour à la liste de categories    #}

    <a href="{{ path('categoreis_index') }}">back to list</a>
{# modifier categorie selon id   #}

    <a href="{{ path('categoreis_edit', {'id': categorei.id}) }}">edit</a>

{# intégrer code de formulaire dans le fichier categoreis/_delete_form.html.twig   #}

    {{ include('categoreis/_delete_form.html.twig') }}
{% endblock %}

{# intégrer code de l'entete dans le fichier _footer.html.twig    #}

{% include '_footer.html.twig' %}